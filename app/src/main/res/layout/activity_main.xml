<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    >

    <com.varunest.sparkbutton.SparkButton
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:id="@+id/spark_button"
        android:layout_centerInParent="true"

        app:sparkbutton_activeImage="@drawable/activity_favorite"
        app:sparkbutton_inActiveImage="@drawable/in_activity_favorite"
        app:sparkbutton_iconSize="40dp"
        app:sparkbutton_primaryColor="@android:color/holo_green_dark"
        app:sparkbutton_secondaryColor="@android:color/holo_orange_dark"/>



         />

</RelativeLayout>